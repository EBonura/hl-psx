[package]
name = "hl-format"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-or-later"
description = "Shared, allocation-free cooked-map contract for hl-psx"

# This crate is consumed by two deliberately separate workspaces: the host BSP
# cooker and the PS1 game. Keep it independent so neither workspace adopts the
# other's target configuration.
[workspace]

[lib]
path = "src/lib.rs"
